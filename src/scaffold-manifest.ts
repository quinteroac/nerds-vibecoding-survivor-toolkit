// AUTO-GENERATED by src/scripts/generate-scaffold-manifest.ts — do not edit manually.
// Re-run `bun run src/scripts/generate-scaffold-manifest.ts` after adding/removing scaffold files.
export const SCAFFOLD_FILES: Array<{ relativePath: string; content: string }> = [
  { relativePath: ".agents/flow/archived/tmpl_.gitkeep", content: "" },
  { relativePath: ".agents/flow/tmpl_it_000001_progress.example.json", content: "{\n  \"entries\": [\n    {\n      \"use_case_id\": \"US-001\",\n      \"timestamp\": \"2026-02-18T12:00:00Z\",\n      \"implementation\": {\n        \"status\": \"pending\",\n        \"summary_of_actions\": \"\",\n        \"learnings\": \"\"\n      },\n      \"tests\": [\n        {\n          \"id\": \"TC-001\",\n          \"description\": \"Example test case\",\n          \"status\": \"pending\"\n        }\n      ]\n    }\n  ]\n}\n" },
  { relativePath: ".agents/skills/approve-prototype/tmpl_SKILL.md", content: "---\nname: approve-prototype\ndescription: \"Drive interactive approval of the prototype by updating project context and roadmap artifacts after a completed iteration.\"\nuser-invocable: false\n---\n\n# Approve Prototype\n\nYou are an **interactive documentation-maintenance agent** that runs when a prototype iteration is ready to be approved.\n\nYour goal is to keep the **project documentation in sync** with the latest iteration by reading the iteration artifacts and then proposing and applying updates to the core context files.\n\nAll generated content **must be in English**.\n\n## Files and artifacts you must use\n\nIteration artifacts live under `.agents/flow/`.\n\nFor the current iteration (`{iteration}`) you **must** read at least:\n\n- `.agents/flow/it_{iteration}_PRD.json` — the approved PRD for this iteration (JSON source of truth).\n- `.agents/flow/it_{iteration}_refactor-report.md` — the refactor completion report produced in the refactor phase (if present).\n- `.agents/flow/it_{iteration}_progress.json` — the prototype progress file (optional but useful for understanding what actually shipped).\n\nYou must also read the long‑lived documentation files at the project root:\n\n- `.agents/PROJECT_CONTEXT.md`\n- `ROADMAP.md` (if it exists)\n- `AGENTS.md` (optional, if it exists)\n- `README.md` (optional, if it exists)\n\n## Required behaviour\n\n### 1. Plan and present changes **before editing anything**\n\nBefore you modify any file, you **must**:\n\n1. Build a concise plan describing:\n   - Which of the following files you intend to modify: `.agents/PROJECT_CONTEXT.md`, `ROADMAP.md`, `AGENTS.md`, `README.md`.\n   - For each file: a short summary of the current state (based on what you read).\n   - For each file: a short summary of the proposed changes (what you will add, remove, or rewrite).\n2. Present this plan to the user **as a clearly separated section**, for example under a heading like `## Planned updates`.\n3. Wait for the user to review and confirm/adjust the plan using the interactive session.\n\nDo **not** edit any file until you have shown this per‑file summary and given the user a chance to react.\n\n### 2. Update `PROJECT_CONTEXT.md` and `ROADMAP.md`\n\nAfter the plan has been presented and adjusted based on user feedback:\n\n1. **Update `.agents/PROJECT_CONTEXT.md`** so that it accurately reflects:\n   - The current architecture, conventions, and technical decisions implied by the latest iteration.\n   - Any newly introduced capabilities, constraints, or quality‑check expectations coming from the iteration artifacts.\n2. **Update `ROADMAP.md`** (creating it if needed) to:\n   - Mark items that were completed in this iteration.\n   - Add or revise upcoming work items derived from gaps, follow‑ups, or technical debt discovered in the PRD, audit, or refactor report.\n\nYour updates must keep the documents coherent, well structured, and easy to read.\n\n### 3. Optionally update `AGENTS.md` and `README.md` when stale\n\nWhen reviewing `AGENTS.md` and `README.md`:\n\n- If their descriptions of the workflow, commands, or capabilities are **clearly out of date** with respect to the current iteration, propose updates as part of your plan.\n- Only modify these files when there is a concrete mismatch or missing information; avoid gratuitous rewrites.\n- When you do update them, describe:\n  - What behaviour or capability changed.\n  - How the new behaviour should be communicated to future developers.\n\n### 4. Apply edits and summarise the outcome\n\nOnce the user has agreed to your plan:\n\n1. Apply the planned edits to the relevant files.\n2. Ensure the result is consistent and that links, section names, and iteration references are correct.\n3. At the end of the session, present a **short summary** of:\n   - Which files were changed.\n   - The most important updates made in each file.\n   - Any follow‑up items that should be addressed in future iterations.\n\n## Context\n\nYou will receive at least:\n\n- `iteration`: the current iteration identifier (e.g. `000027`).\n\nUse this to locate the iteration artifacts under `.agents/flow/`:\n\n- `.agents/flow/it_{iteration}_PRD.json`\n- `.agents/flow/it_{iteration}_refactor-report.md` (if present)\n- `.agents/flow/it_{iteration}_progress.json`\n\nAlways follow the workflow phases and conventions described in the existing `.agents/PROJECT_CONTEXT.md`.\n" },
  { relativePath: ".agents/skills/audit-prototype/tmpl_SKILL.md", content: "---\nname: audit-prototype\ndescription: \"Validate the current iteration's PRD against the implemented code via the audit prototype skill.\"\nuser-invocable: false\n---\n\n# Audit Prototype\n\nValidate the product requirement document (PRD) for the current iteration against the implemented code.\n\n## Context\n\n- Iteration artifacts live under `.agents/flow/`.\n- Relevant files:\n  - `.agents/flow/it_{iteration}_PRD.json` — PRD (user stories, acceptance criteria, functional requirements)\n  - `.agents/flow/it_{iteration}_progress.json` — implementation progress\n\n## Task\n\nFor each use case / user story and its acceptance criteria (and any referenced functional requirements), validate that the codebase satisfies the PRD. Report any gaps or non-compliance.\n\n## Output: Compliance report (mandatory structure)\n\nProduce a **compliance report** with the following fixed structure so the user receives a consistent summary and per-FR / per-US verification. Write the report as JSON to:\n\n`.agents/flow/it_{iteration}_compliance-report.json`\n\nThe report must include exactly these sections:\n\n1. **Executive summary** — string.\n2. **Verification by FR** — array of `{ \"frId\": \"<id>\", \"assessment\": \"<value>\" }` for each functional requirement. Assessment must be one of: `\"comply\"`, `\"partially_comply\"`, `\"does_not_comply\"` (or in Spanish: cumple / parcialmente cumple / no cumple — use English keys in JSON).\n3. **Verification by US** — array of `{ \"usId\": \"<id>\", \"assessment\": \"<value>\" }` for each user story. Assessment must be one of: `\"comply\"`, `\"partially_comply\"`, `\"does_not_comply\"`.\n4. **Minor observations** — array of strings (zero or more).\n5. **Conclusions and recommendations** — string.\n\nEach FR and US from the PRD must be explicitly assessed with one of: comply / partially comply / does not comply (English), or cumple / parcialmente cumple / no cumple if the surrounding document is in Spanish. In the JSON file use the English enum values: `\"comply\"`, `\"partially_comply\"`, `\"does_not_comply\"`.\n\n## After the report: choose how to act on recommendations\n\nAfter presenting the compliance report to the user, ask them to choose one of:\n\n- **(a) Follow recommendations** — apply the recommendations as-is (e.g. proceed to refactor).\n- **(b) Change recommendations** — adjust the recommendations before proceeding.\n- **(c) Leave as is** — do not apply; optionally record items as technical debt.\n\nIf the user chooses **(b)**, ask what they want to change, then update the recommendations accordingly (e.g. revise the conclusions and recommendations text or the refactor plan) before continuing.\n\n## Outcome-driven artifacts\n\nUse the chosen outcome to drive which artifacts you produce:\n\n1. **Always** — After the user's choice is known, produce the audit artifact so the result is persisted and reproducible. Write the file to **`.agents/flow/it_{iteration}_audit.md`** with the mandatory structure below.\n2. **When the user chooses to follow or apply refactor** (option a or b with refactor intent) — Produce `.agents/flow/it_{iteration}_audit.json` (e.g. via `nvst write-json` if a schema exists, or a structured JSON consistent with the audit content).\n3. **When the user marks items as technical debt** (option c or explicit \"leave as debt\") — Update `.agents/TECHNICAL_DEBT.md` (or the project's designated technical-debt file) with those items. Run the NVST command so the file is updated by the toolkit: `bun nvst write-technical-debt --data '<json>'` (or pipe JSON via stdin). The payload must be `{ \"iteration\": \"<current_iteration>\", \"items\": [ { \"title\": \"<short title>\", \"description\": \"<optional description>\" }, ... ] }`. Use `--out <path>` only if the project uses a different technical-debt file path. Updates are additive; existing debt entries are preserved.\n\n### Mandatory structure for it_{iteration}_audit.md\n\nThe file must be written to `.agents/flow/it_{iteration}_audit.md` and must contain exactly these sections (in this order), reflecting the final recommendations (including any user-requested changes):\n\n1. **Executive summary**\n2. **Verification by FR**\n3. **Verification by US**\n4. **Minor observations**\n5. **Conclusions and recommendations**\n6. **Refactor plan**\n" },
  { relativePath: ".agents/skills/create-pr-document/tmpl_SKILL.md", content: "---\nname: create-pr-document\ndescription: \"Gathers the requirement from the user and produces it_{iteration}_product-requirement-document.md. Triggered by: bun nvst define requirement.\"\nuser-invocable: true\n---\n\n# Create Product Requirement Document\n\nProduce `it_{current_iteration}_product-requirement-document.md` in `.agents/flow/` by interviewing the user about the feature or change they want to build.\n\n**Important:** Do NOT start implementing. Just gather the requirement and write the document.\n\n---\n\n## The Job\n\n1. **Understand the project first.** Read `AGENTS.md`, `.agents/PROJECT_CONTEXT.md`, and explore the codebase structure (main entry points, conventions, existing features) before starting the interview. This context will make your questions more relevant and the PRD better aligned with the project.\n2. Read `state.json` to get `current_iteration` (6-digit string, e.g. `\"000001\"`).\n3. Ask 3–5 clarifying questions (see Questions Flow).\n4. Generate the document following the Output Structure.\n5. Write to `.agents/flow/it_{current_iteration}_product-requirement-document.md`.\n6. Update `state.json`: `requirement_definition.status` = `\"in_progress\"`, `requirement_definition.file` = filename.\n\n---\n\n## Questions Flow\n\n**CRITICAL: Ask ONE question at a time. Wait for the user's answer before asking the next question. Do NOT present all questions at once.**\n\nAsk only questions where the initial prompt is ambiguous. Present lettered options so the user can reply with short codes (e.g. \"1A\").\n\nQuestions to ask (one by one, in order):\n\n1. What problem does this solve or goal does it achieve?\n   - A. [inferred option]\n   - B. [inferred option]\n   - C. Other: [please specify]\n\n*(Wait for answer, then ask question 2)*\n\n2. Who is the primary user or actor?\n   - A. End user / customer\n   - B. Internal operator / admin\n   - C. Another system or automated process\n   - D. Other: [specify]\n\n*(Wait for answer, then ask question 3)*\n\n3. MVP scope — what is the minimum set of use cases needed to validate the idea?\n   List only the user stories you consider strictly necessary.\n   *(e.g. \"UC-1: user can log in, UC-2: user can view dashboard\")*\n\n*(Wait for answer, then ask question 4)*\n\n4. Are there hard constraints (deadline, platform, dependencies)?\n   *(Skip if the user says none)*\n\n*(Wait for answer, then ask question 5)*\n\n5. What does \"done\" look like? How will we know it works?\n   *(Describe acceptance criteria or how you'd verify success)*\n\n*(Wait for answer, then generate the document)*\n\n---\n\n## Output Structure\n\n```markdown\n# Requirement: [Feature or Change Name]\n\n## Context\nBrief description of the problem or opportunity this addresses.\n\n## Goals\n- [Specific, measurable objective]\n- …\n\n## User Stories\nEach story must be small enough to implement in one focused session.\n\n### US-001: [Title]\n**As a** [actor], **I want** [capability] **so that** [benefit].\n\n**Acceptance Criteria:**\n- [ ] [Specific, verifiable criterion — not vague]\n- [ ] [Another criterion]\n- [ ] Typecheck / lint passes\n- [ ] **[UI stories only]** Visually verified in browser\n\n### US-002: …\n\n## Functional Requirements\n- FR-1: …\n- FR-2: …\n\n## Non-Goals (Out of Scope)\n- …\n\n## Open Questions\n- …\n```\n\n---\n\n## Writing Guidelines\n\n- **MVP first:** include only the user stories from the answer to question 3. If the user did not list explicit stories, propose the smallest set possible and confirm before writing. Do not pad scope.\n- Be explicit and unambiguous — the reader may be a junior developer or an AI agent.\n- Acceptance criteria must be verifiable: \"button shows confirmation dialog before deleting\" ✓ — \"works correctly\" ✗.\n- Every story with a UI change must include browser verification as an acceptance criterion.\n- Number requirements (`FR-N`) for easy cross-reference with `it_{iteration}_PRD.json`.\n\n---\n\n---\n\n## After writing the PRD: Resolve open questions\n\n**CRITICAL: Do this at the end of the session, after you have written the PRD file and before you update `state.json`.**\n\n1. **Detect open questions.** Read the PRD file you just wrote (`.agents/flow/it_{current_iteration}_product-requirement-document.md`). If it contains a section `## Open Questions` with one or more list items (lines starting with `-` or `*` or a number), treat each list item as an open question. Skip placeholders such as \"None\", \"…\", or \"TBD\" — only ask items that are real questions.\n2. **Ask the open questions one by one.** For each open question (in order), ask the user that question. **When asking each open question, always include valid suggestions or inferred options** (e.g. lettered choices A, B, C, or \"Other: [specify]\") so the user has helpful context to provide an answer. Infer options from the PRD context when possible; if none fit, offer at least 2–3 plausible options plus \"Other\".\n3. **Wait for the user's answer before proceeding.** After asking a question, wait for the user to respond. Only then ask the next question (or, if there are no more, proceed to update `state.json` and finish).\n\nIf there are no open questions (section missing or only placeholders), skip this block and go straight to updating `state.json`.\n\n---\n\n## Checklist\n\nBefore saving:\n\n- [ ] Clarifying questions asked and answered\n- [ ] Each user story has verifiable acceptance criteria\n- [ ] Functional requirements are numbered and unambiguous\n- [ ] Non-goals define clear scope boundaries\n- [ ] File written to `.agents/flow/it_{current_iteration}_product-requirement-document.md`\n- [ ] **Open questions (if any) asked one by one with suggestions/options per question, and user answers collected**\n- [ ] `state.json` → `requirement_definition.status` = `\"in_progress\"`, `requirement_definition.file` set" },
  { relativePath: ".agents/skills/implement-user-story/tmpl_SKILL.md", content: "---\nname: implement-user-story\ndescription: \"Implements a single user story from the PRD: writes code and tests, follows project conventions. Invoked by: bun nvst create prototype.\"\nuser-invocable: false\n---\n\n# Implement User Story\n\nImplement the provided user story by writing production code and tests that satisfy all acceptance criteria, following the project's conventions and architecture.\n\n---\n\n## The Job\n\n1. Read the **user story** and its **acceptance criteria** carefully.\n2. Review the **project context** to understand conventions, tech stack, testing strategy, and module structure.\n3. Plan the implementation: identify which files to create or modify, what tests to write, and how the change fits into the existing architecture.\n4. Implement the user story:\n   - Write production code that satisfies every acceptance criterion.\n   - Write tests that verify each acceptance criterion (follow the testing strategy from the project context).\n   - Follow all naming conventions, code standards, and forbidden patterns from the project context.\n5. Verify your work:\n   - Ensure the code compiles / type-checks without errors.\n   - Run any quality checks defined in the project context.\n   - Fix any issues before finishing.\n6. Do **not** commit — the calling command handles git commits.\n\n---\n\n## Inputs\n\n| Source | Used for |\n|--------|----------|\n| `user_story` (context variable) | The user story JSON with id, title, description, and acceptanceCriteria |\n| `project_context` (context variable) | Project conventions, tech stack, code standards, testing strategy, and architecture |\n| `iteration` (context variable) | Current iteration number for file naming and context |\n\n---\n\n## Rules\n\n- **One story at a time.** Implement only the user story provided — do not implement other stories or make unrelated changes.\n- **Follow conventions exactly.** Use the naming, formatting, error handling, and module organisation patterns from the project context.\n- **Test every acceptance criterion.** Each AC should have at least one corresponding test assertion.\n- **No new dependencies** unless the acceptance criteria explicitly require them.\n- **Do not modify state files.** Do not touch `.agents/state.json` or progress files — the calling command manages those.\n- **Do not commit.** The calling command will commit after verifying quality checks pass.\n- **Keep changes minimal.** Only modify files necessary to implement the user story. Do not refactor unrelated code.\n\n---\n\n## Output\n\nThe output is the set of file changes (new files created, existing files modified) in the working tree. There is no document to produce — the code and tests are the deliverable.\n\n---\n\n## Checklist\n\nBefore finishing:\n\n- [ ] All acceptance criteria from the user story are implemented\n- [ ] Tests cover each acceptance criterion\n- [ ] Code follows project conventions (naming, style, error handling)\n- [ ] Code compiles / type-checks without errors\n- [ ] No unrelated changes were made\n- [ ] No state files were modified\n- [ ] No git commits were made\n" },
  { relativePath: ".agents/skills/refactor-prototype/tmpl_SKILL.md", content: "---\nname: refactor-prototype\ndescription: \"Load the refactor plan from it_{iteration}_audit.json and invoke the agent to apply code changes.\"\nuser-invocable: false\n---\n\n# Refactor Prototype\n\nApply the refactor plan produced by the audit phase to the codebase in a **single, autonomous agent session**.\n\n## Your task\n\n1. **Read the audit JSON** at the path provided in the Context section below (`audit_json_path`). This file contains the refactor plan (e.g. goals, user stories, refactor items, and quality checks) from `nvst audit prototype`.\n\n2. **Understand the refactor plan** — goals, recommended changes, any structured refactor items, and any specified quality checks or validation commands.\n\n3. **Apply all recommended code changes** — implement each refactor item in the codebase. Follow project conventions and the existing architecture. Do not leave any planned change partially applied.\n\n4. **Run the quality checks defined in the refactor plan** — for each quality check or command listed in the plan, run it and fix any issues until the checks pass. At minimum, ensure the project's typecheck (`bun run typecheck`) and test suite (`bun test` when appropriate) succeed before finishing.\n\n5. **Perform the full refactor autonomously** — do not stop mid-way to ask the user what to do next or whether to continue. Use the refactor plan and the existing codebase as your source of truth, carry out the entire refactor in this single session, and only use interaction (if any) to report progress and final status.\n\n6. **Write a completion report artifact** — after all refactor changes have been applied and quality checks have passed, write a markdown file named `it_{iteration}_refactor-report.md` into the `.agents/flow/` directory at the project root. This file is the completion indicator used by downstream steps to verify that the refactor finished.\n\n   The report **must be in English** and include, at minimum, the following top-level sections with meaningful content:\n\n   - `## Summary of changes` — a concise summary of the key refactor changes you implemented.\n   - `## Quality checks` — which checks you ran (including `bun run typecheck` and `bun test` when appropriate), their outcomes, and any important notes.\n   - `## Deviations from refactor plan` — describe any deviations from the original refactor plan in the audit JSON. If there were no deviations, explicitly write `None`.\n\n## Context\n\nYou will receive:\n\n- `iteration`: current iteration (e.g. `000026`).\n- `audit_json_path`: absolute path to `it_{iteration}_audit.json` in `.agents/flow/`. Read this file to get the refactor plan and quality checks.\n\nFrom the project root, you must use the following iteration artifacts under `.agents/flow/` as your primary sources of truth:\n\n- `.agents/flow/it_{iteration}_PRD.json` — the approved PRD for this iteration (JSON source of truth).\n- `.agents/flow/it_{iteration}_progress.json` — the prototype progress file that reflects what actually shipped.\n\nUse the audit JSON as the single source of truth for what to refactor; then apply all changes and verify with the project's quality checks in this single run, finishing by writing the refactor completion report described above.\n" },
  { relativePath: ".agents/skills/refine-pr-document/tmpl_SKILL.md", content: "---\nname: refine-pr-document\ndescription: \"Updates an existing product requirement document based on user feedback. Triggered by: bun nvst refine requirement.\"\nuser-invocable: true\n---\n\n# Refine Product Requirement Document\n\nUpdate `it_{current_iteration}_product-requirement-document.md` in place based on user feedback. The file must already exist; this skill does not create it from scratch.\n\n**Do NOT start implementing. Only update the document.**\n\n> **Two modes available — the agent asks at the start:**\n> - **Editor mode** — apply specific changes requested by the user.\n> - **Challenger mode** — act as an independent critical reviewer: challenge assumptions, question scope, find gaps, and propose improvements. The user accepts or rejects each suggestion before anything is written.\n\n---\n\n## The Job\n\n1. Read `state.json` → get `requirement_definition.file` (e.g. `it_000001_product-requirement-document.md`).\n2. Read the current document from `.agents/flow/{file}`.\n3. **Open by asking:** _\"Would you like me to challenge the existing document as an independent reviewer, or would you prefer to tell me what to change?\"_\n   - Answer → **challenge**: run Challenger Mode (see below).\n   - Answer → **edit**: run Editor Mode (see Questions Flow).\n4. Apply changes to the document following the same Output Structure as `create-pr-document`.\n5. Re-enforce MVP constraint: remove any user stories not explicitly listed by the user; do not add new ones without confirmation.\n6. Write the updated file back to the same path.\n7. Do **not** update `state.json` — `requirement_definition.status` stays `\"in_progress\"`.\n\n---\n\n## Challenger Mode\n\nAct as a second agent reviewing the document with fresh eyes. Do not apply any change without explicit user approval.\n\n**Challenge areas — examine each (in order):**\n\n1. **Assumptions** — List implicit assumptions in the goals or stories. Ask: are these validated? what if they are wrong?\n2. **Scope creep** — Flag any user story that is not strictly MVP-necessary. Propose removal and ask for confirmation.\n3. **Vague acceptance criteria** — Highlight criteria that are not concretely verifiable. Suggest a specific rewrite.\n4. **Missing non-goals** — Identify things the document is silent on that could be misread as in-scope. Propose additions to Non-Goals.\n5. **Missing edge cases** — Point out failure paths or user states not covered by any acceptance criterion.\n6. **Conflicting requirements** — Flag any `FR-N` that contradicts another or contradicts an acceptance criterion.\n\n**Challenger output format — one observation at a time:**\n\n> CRITICAL: Present findings **one at a time**. After delivering one finding, stop and wait for the user to respond before moving on. Do not queue or batch findings.\n\nFor each finding, format it as:\n\n```\nChallenge [N/total]: <area name>\n\nFinding: <what the issue is>\nSuggestion: <proposed change>\n\nAccept / Reject / Discuss?\n```\n\n- After the user responds, acknowledge their decision and immediately present the **next** finding.\n- If the user types **Discuss**, engage in a short back-and-forth until they give a final Accept or Reject, then move on.\n- Once all findings have been reviewed, summarise the accepted changes and apply them to the file in a single write.\n- Do **not** write anything to the file until all findings have received a response.\n\n---\n\n## Editor Mode (Questions Flow)\n\nAsk only what is needed to understand the requested change.\n\n```\n1. What would you like to change or add?\n   A. Replace or rewrite a user story\n   B. Add a new user story (must be MVP-justified)\n   C. Remove a user story\n   D. Tighten acceptance criteria\n   E. Update goals or non-goals\n   F. Other: [describe]\n\n2. If adding a user story — is it strictly necessary for the MVP?\n   [Open answer — skip if not adding stories]\n\n3. Anything else to update in this pass?\n   [Open answer — skip if none]\n```\n\n---\n\n## Refinement Rules\n\n- **MVP constraint holds:** do not add user stories unless the user explicitly requests them and confirms they are MVP-necessary.\n- **Preserve numbering:** renumber `US-NNN` and `FR-N` entries only if a story is removed; otherwise keep existing IDs stable.\n- **Verifiable criteria:** any new or edited acceptance criterion must be concrete and testable (same standard as `create-pr-document`).\n- **UI stories:** if a new or edited story touches the UI, \"visually verified in browser\" must be an acceptance criterion.\n\n---\n\n## Checklist\n\nBefore saving:\n\n- [ ] User's requested changes applied\n- [ ] No new user stories added without explicit MVP justification\n- [ ] All acceptance criteria remain verifiable\n- [ ] `US-NNN` and `FR-N` numbering is consistent\n- [ ] File written back to `.agents/flow/it_{current_iteration}_product-requirement-document.md`\n- [ ] `state.json` **not** modified (status stays `\"in_progress\"`)\n" },
  { relativePath: ".agents/tmpl_PROJECT_CONTEXT.md", content: "# Project context\n\n<!-- Created or updated by `bun nvst create project-context`. Conventions, stack, and product architecture. Cap 250 lines (see process_design summary mechanism). -->\n" },
  { relativePath: ".agents/tmpl_state.example.json", content: "{\n  \"current_iteration\": \"000001\",\n  \"current_phase\": \"define\",\n  \"phases\": {\n    \"define\": {\n      \"requirement_definition\": { \"status\": \"pending\", \"file\": null },\n      \"prd_generation\": { \"status\": \"pending\", \"file\": null }\n    },\n    \"prototype\": {\n      \"prototype_creation\": { \"status\": \"pending\", \"file\": null },\n      \"prototype_audit\": { \"status\": \"pending\", \"file\": null },\n      \"prototype_refactor\": { \"status\": \"pending\", \"file\": null },\n      \"prototype_approval\": { \"status\": \"pending\", \"file\": null },\n      \"project_context\": { \"status\": \"pending\", \"file\": null },\n      \"test_plan\": { \"status\": \"pending\", \"file\": null },\n      \"tp_generation\": { \"status\": \"pending\", \"file\": null },\n      \"prototype_build\": { \"status\": \"pending\", \"file\": null },\n      \"test_execution\": { \"status\": \"pending\", \"file\": null },\n      \"prototype_approved\": false\n    },\n    \"refactor\": {\n      \"evaluation_report\": { \"status\": \"pending\", \"file\": null },\n      \"refactor_plan\": { \"status\": \"pending\", \"file\": null },\n      \"refactor_execution\": { \"status\": \"pending\", \"file\": null },\n      \"changelog\": { \"status\": \"pending\", \"file\": null }\n    }\n  },\n  \"deprecated_notes\": [\n    \"prototype.project_context, prototype.test_plan, prototype.tp_generation, prototype.prototype_build, prototype.test_execution, prototype.prototype_approved, and phases.refactor are deprecated and retained only for backward compatibility.\"\n  ],\n  \"last_updated\": \"2026-02-18T12:00:00Z\",\n  \"history\": []\n}\n" },
  { relativePath: ".agents/tmpl_state.json", content: "{\n  \"current_iteration\": \"000001\",\n  \"current_phase\": \"define\",\n  \"phases\": {\n    \"define\": {\n      \"requirement_definition\": { \"status\": \"pending\", \"file\": null },\n      \"prd_generation\": { \"status\": \"pending\", \"file\": null }\n    },\n    \"prototype\": {\n      \"prototype_creation\": { \"status\": \"pending\", \"file\": null },\n      \"prototype_audit\": { \"status\": \"pending\", \"file\": null },\n      \"prototype_refactor\": { \"status\": \"pending\", \"file\": null },\n      \"prototype_approval\": { \"status\": \"pending\", \"file\": null }\n    },\n    \"refactor\": {}\n  },\n  \"last_updated\": \"2026-01-01T00:00:00.000Z\",\n  \"history\": []\n}\n" },
  { relativePath: ".agents/tmpl_state_rules.md", content: "# State validation rules (scaffold)\n\n<!-- TODO: Fill in preconditions and postconditions per command. All content in English. -->\n\nEach command must validate the relevant fields of `state.json` before running. If the current state does not allow the command, the command fails with a clear message.\n\n| Command | Pre | Post |\n|--------|-----|-----|\n| `bun start iteration` | TBD | TBD |\n| `bun define requirement` | TBD | TBD |\n| `bun refine requirement` | TBD | TBD |\n| `bun approve requirement` | TBD | TBD |\n| `bun create prd` | TBD | TBD |\n| `bun create project-context` | TBD | TBD |\n| `bun approve project-context` | TBD | TBD |\n| `bun create prototype` | TBD | TBD |\n| `bun define test-plan` | TBD | TBD |\n| `bun refine test-plan` | TBD | TBD |\n| `bun approve test-plan` | TBD | TBD |\n| `bun execute test-plan` | TBD | TBD |\n| `bun execute automated-fix` | TBD | TBD |\n| `bun execute manual-fix` | TBD | TBD |\n| `bun define refactor-plan` | TBD | TBD |\n| `bun refine refactor-plan` | TBD | TBD |\n| `bun approve refactor-plan` | TBD | TBD |\n| `bun create prd --refactor` | TBD | TBD |\n| `bun execute refactor` | TBD | TBD |\n| `bun register changelog` (or equivalent) | TBD | TBD |\n" },
  { relativePath: "docs/nvst-flow/templates/tmpl_CHANGELOG.md", content: "# Changelog\n\n<!-- TODO: All content in English. One block per iteration. -->\n\n## [Unreleased]\n\n<!-- TODO -->\n\n## Iteration 000001 — YYYY-MM-DD\n\n<!-- TODO: Summary of requirements, refactorings, fixes. -->\n\n---\n\n<!-- Template for next iteration:\n## Iteration 000002 — YYYY-MM-DD\n...\n-->\n" },
  { relativePath: "docs/nvst-flow/templates/tmpl_TECHNICAL_DEBT.md", content: "# Technical debt (discarded or deferred)\n\n<!-- TODO: All content in English. Updated when approving refactor plan (bun approve refactor-plan). Used as input in future iteration evaluations. -->\n\n## From iteration 000001\n\n<!-- TODO: Items not included in the refactor plan; rationale optional. -->\n\n---\n\n<!-- Add new section per iteration when refactor plan is approved. -->\n" },
  { relativePath: "docs/nvst-flow/templates/tmpl_it_000001_evaluation-report.md", content: "# Evaluation report — Iteration 000001\n\n<!-- TODO: Replace 000001 with current iteration. Complete sections. All content in English. -->\n\n## Strengths\n\n<!-- TODO -->\n\n## Technical debt\n\n<!-- TODO: Items to consider for refactor. -->\n\n## Violations of PROJECT_CONTEXT.md\n\n<!-- TODO -->\n\n## Recommendations\n\n<!-- TODO: By impact, urgency, effort, scope; optional numeric score for ordering. -->\n" },
  { relativePath: "docs/nvst-flow/templates/tmpl_it_000001_product-requirement-document.md", content: "# Product requirement document — Iteration 000001\n\n<!-- TODO: Replace 000001 with current iteration. Complete sections. All content in English. -->\n\n## Summary\n\n<!-- TODO -->\n\n## Requirements\n\n<!-- TODO: List requirements / user stories. -->\n\n## Acceptance criteria\n\n<!-- TODO -->\n\n## Out of scope (this iteration)\n\n<!-- TODO -->\n" },
  { relativePath: "docs/nvst-flow/templates/tmpl_it_000001_refactor_plan.md", content: "# Refactor plan — Iteration 000001\n\n<!-- TODO: Replace 000001 with current iteration. Complete sections. All content in English. -->\n\n## Quick wins\n\n<!-- TODO -->\n\n## Critical refactorings\n\n<!-- TODO -->\n\n## Deferred / discarded (→ TECHNICAL_DEBT.md)\n\n<!-- TODO -->\n\n## Order of execution\n\n<!-- TODO: Ordered list of refactor steps. -->\n" },
  { relativePath: "docs/nvst-flow/templates/tmpl_it_000001_test-plan.md", content: "# Test plan — Iteration 000001\n\n<!-- TODO: Replace 000001 with current iteration. Complete sections. All content in English. -->\n\n## Scope\n\n<!-- TODO: What is in scope for testing this iteration. -->\n\n## Automated tests\n\n<!-- TODO: List test cases; link to use cases / progress.json entries. -->\n\n## Exploratory / manual tests\n\n<!-- TODO: UX, exploratory; owned by user. -->\n\n## Environment and data\n\n<!-- TODO if needed -->\n" },
  { relativePath: "docs/nvst-flow/tmpl_COMMANDS.md", content: "# NVST Command Reference\n\nFull reference for the `nvst` CLI commands. See [QUICK_USE.md](QUICK_USE.md) for common workflows.\n\n## Main Development Loop\n\n`Define/Refine/Approve Requirement → Create Prototype → Audit Prototype → Refactor Prototype → Approve Prototype`\n\nCommand form:\n\n`define requirement` → `refine requirement` (optional) → `approve requirement` → `create prototype` → `audit prototype` → `refactor prototype` → `approve prototype`\n\n## Lifecycle\n\n| Command | Description |\n|---------|-------------|\n| `nvst init` | Initialize toolkit files in the current directory (copies scaffold from package) |\n| `nvst destroy [--clean]` | Remove files generated by nvst. Use `--clean` to also remove `.agents/flow/archived` |\n\n## Main Loop Commands\n\n| Command | Description |\n|---------|-------------|\n| `nvst define requirement --agent <provider> [--force]` | Create requirement document via agent |\n| `nvst refine requirement --agent <provider> [--challenge] [--force]` | Refine requirement document (editor or challenger mode) |\n| `nvst approve requirement [--force]` | Mark requirement definition as approved |\n| `nvst create prototype --agent <provider> [--iterations <N>] [--retry-on-fail <N>] [--stop-on-critical] [--force]` | Iterative agent-driven implementation of user stories |\n| `nvst audit prototype --agent <provider> [--force]` | Run prototype audit step |\n| `nvst refactor prototype --agent <provider> [--force]` | Run prototype refactor step |\n| `nvst approve prototype [--force]` | Stage and commit all pending changes for current iteration |\n\n## Utilities\n\n| Command | Description |\n|---------|-------------|\n| `nvst write-json --schema <name> --out <path> [--data '<json>']` | Write a schema-validated JSON file (payload via --data or stdin) |\n\n## Global Options\n\n| Option | Description |\n|--------|-------------|\n| `--agent <provider>` | Agent provider: `claude`, `codex`, `gemini`, `cursor` |\n| `--iterations <N>` | Maximum prototype/fix passes (integer >= 1) |\n| `--retry-on-fail <N>` | Retry attempts per failed story (integer >= 0) |\n| `--stop-on-critical` | Stop execution after critical failures |\n| `--force` | Bypass flow guardrail confirmation |\n| `--challenge` | Run refine in challenger mode (critical reviewer) |\n| `--clean` | With destroy: also removes .agents/flow/archived |\n| `-h`, `--help` | Show help message |\n| `-v`, `--version` | Print version and exit |\n" },
  { relativePath: "docs/nvst-flow/tmpl_QUICK_USE.md", content: "# NVST Quick Use\n\nQuick reference for the main development loop. See [COMMANDS.md](COMMANDS.md) for full command reference.\n\n**Running commands:** Use `bun nvst` so Bun resolves the binary. Or add `node_modules/.bin` to your PATH: `export PATH=\"$PATH:$(pwd)/node_modules/.bin\"` to run `nvst` directly.\n\n## First-time setup\n\n```bash\n# Install the toolkit from npm (package is published)\nnpm install @quinteroac/agents-coding-toolkit\n# or with Bun\nbun add @quinteroac/agents-coding-toolkit\n\n# From local path (development)\n# bun add /path/to/nerds-vibecoding-survivor-toolkit\n\n# Initialize scaffold in your project\nbun nvst init\n```\n\n## Typical iteration flow (end-to-end)\n\n```bash\nbun nvst define requirement --agent codex\nbun nvst refine requirement --agent codex --challenge   # optional\nbun nvst approve requirement\nbun nvst create prototype --agent codex --iterations 10\nbun nvst audit prototype --agent codex\nbun nvst refactor prototype --agent codex\nbun nvst approve prototype\n```\n\nUse this command order as the standard loop:\n\n`Define/Refine/Approve Requirement → Create Prototype → Audit Prototype → Refactor Prototype → Approve Prototype`\n\n## Agent providers\n\nUse `--agent` with: `claude`, `codex`, `gemini`, or `cursor`.\n\nExample:\n\n```bash\nbun nvst define requirement --agent cursor\n```\n" },
  { relativePath: "schemas/node-shims.d.ts", content: "declare module \"fs\" {\n  export function readFileSync(path: string, encoding: string): string;\n}\n\ndeclare module \"path\" {\n  export function join(...paths: string[]): string;\n}\n\ndeclare const process: {\n  exitCode?: number;\n};\n\ninterface ImportMeta {\n  readonly dir: string;\n}\n" },
  { relativePath: "schemas/tmpl_audit-artifact.ts", content: "import { z } from \"zod\";\n\nimport { ComplianceAssessmentSchema } from \"./tmpl_compliance-report\";\n\nconst VerificationByFrEntrySchema = z.object({\n  frId: z.string().min(1),\n  assessment: ComplianceAssessmentSchema,\n});\n\nconst VerificationByUsEntrySchema = z.object({\n  usId: z.string().min(1),\n  assessment: ComplianceAssessmentSchema,\n});\n\nconst RefactorPlanItemSchema = z.object({\n  id: z.string().min(1),\n  title: z.string().min(1),\n  description: z.string().min(1),\n  priority: z.enum([\"low\", \"medium\", \"high\"]).optional(),\n});\n\nconst RefactorPlanSchema = z.object({\n  mandatoryItems: z.array(RefactorPlanItemSchema),\n  optionalItems: z.array(RefactorPlanItemSchema),\n});\n\n/**\n * Structured schema for the JSON audit artifact written as\n * `.agents/flow/it_{iteration}_audit.json`.\n *\n * This complements the human-readable Markdown report and makes the\n * refactor plan (including mandatory/optional items) machine-consumable\n * for the `refactor-prototype` skill and other tooling.\n */\nexport const AuditArtifactSchema = z.object({\n  iteration: z.string().min(1),\n  executiveSummary: z.string(),\n  verificationByFR: z.array(VerificationByFrEntrySchema),\n  verificationByUS: z.array(VerificationByUsEntrySchema),\n  minorObservations: z.array(z.string()),\n  conclusionsAndRecommendations: z.string(),\n  refactorPlan: RefactorPlanSchema,\n});\n\nexport type AuditArtifact = z.infer<typeof AuditArtifactSchema>;\n\n" },
  { relativePath: "schemas/tmpl_audit.ts", content: "import { z } from \"zod\";\n\nconst acceptanceCriterion = z.object({\n  id: z.string(),\n  text: z.string(),\n});\n\nconst userStory = z.object({\n  id: z.string(),\n  title: z.string(),\n  description: z.string(),\n  acceptanceCriteria: z.array(acceptanceCriterion),\n});\n\nconst functionalRequirement = z.object({\n  id: z.string(),\n  description: z.string(),\n});\n\nexport const AuditSchema = z.object({\n  goals: z.array(z.string()),\n  userStories: z.array(userStory),\n  functionalRequirements: z.array(functionalRequirement),\n  refactorPlan: z.string().optional(),\n});\n\nexport type Audit = z.infer<typeof AuditSchema>;\n" },
  { relativePath: "schemas/tmpl_compliance-report.ts", content: "import { z } from \"zod\";\n\n/**\n * Assessment value for each FR and US.\n * Equivalent labels in Spanish: cumple / parcialmente cumple / no cumple.\n */\nexport const ComplianceAssessmentSchema = z.enum([\n  \"comply\",\n  \"partially_comply\",\n  \"does_not_comply\",\n]);\nexport type ComplianceAssessment = z.infer<typeof ComplianceAssessmentSchema>;\n\nconst VerificationByFrEntrySchema = z.object({\n  frId: z.string().min(1),\n  assessment: ComplianceAssessmentSchema,\n});\n\nconst VerificationByUsEntrySchema = z.object({\n  usId: z.string().min(1),\n  assessment: ComplianceAssessmentSchema,\n});\n\n/**\n * Mandatory structure for the audit compliance report (US-002).\n * The agent must produce a report with these sections so that the user\n * receives a consistent summary and per-FR / per-US verification.\n */\nexport const ComplianceReportSchema = z.object({\n  executiveSummary: z.string(),\n  verificationByFr: z.array(VerificationByFrEntrySchema),\n  verificationByUs: z.array(VerificationByUsEntrySchema),\n  minorObservations: z.array(z.string()),\n  conclusionsAndRecommendations: z.string(),\n});\n\nexport type ComplianceReport = z.infer<typeof ComplianceReportSchema>;\n" },
  { relativePath: "schemas/tmpl_issues.ts", content: "import { z } from \"zod\";\n\nconst IssueSchema = z.object({\n  id: z.string(),\n  title: z.string(),\n  description: z.string(),\n  status: z.enum([\"open\", \"fixed\", \"retry\", \"manual-fix\"]),\n});\n\nexport const IssuesSchema = z.array(IssueSchema).refine(\n  (issues) => {\n    const ids = issues.map((i) => i.id);\n    return new Set(ids).size === ids.length;\n  },\n  { message: \"Issue IDs must be unique\" },\n);\n\nexport type Issue = z.infer<typeof IssueSchema>;\nexport type Issues = z.infer<typeof IssuesSchema>;\n" },
  { relativePath: "schemas/tmpl_prd.ts", content: "import { z } from \"zod\";\n\nconst acceptanceCriterion = z.object({\n    id: z.string(),\n    text: z.string(),\n});\n\nconst userStory = z.object({\n    id: z.string(),\n    title: z.string(),\n    description: z.string(),\n    acceptanceCriteria: z.array(acceptanceCriterion),\n});\n\nconst functionalRequirement = z.object({\n    id: z.string(),\n    description: z.string(),\n});\n\nexport const PrdSchema = z.object({\n    goals: z.array(z.string()),\n    userStories: z.array(userStory),\n    functionalRequirements: z.array(functionalRequirement),\n});\n\nexport type Prd = z.infer<typeof PrdSchema>;\n" },
  { relativePath: "schemas/tmpl_progress.ts", content: "import { z } from \"zod\";\n\nconst implementationStatus = z.enum([\"pending\", \"in_progress\", \"completed\"]);\nconst testStatus = z.enum([\n  \"pending\",\n  \"written\",\n  \"passed\",\n  \"failed\",\n  \"fixed\",\n  \"unfixed\",\n]);\n\nconst implementation = z.object({\n  status: implementationStatus,\n  summary_of_actions: z.string(),\n  learnings: z.string(),\n});\n\nconst testEntry = z.object({\n  id: z.string(),\n  description: z.string(),\n  status: testStatus,\n  file: z.string().optional(),\n  last_run: z.string().optional(),\n  error: z.string().optional(),\n});\n\nconst progressEntry = z.object({\n  use_case_id: z.string(),\n  timestamp: z.string(),\n  implementation,\n  tests: z.array(testEntry),\n});\n\nexport const ProgressSchema = z.object({\n  entries: z.array(progressEntry),\n});\n\nexport type Progress = z.infer<typeof ProgressSchema>;\n" },
  { relativePath: "schemas/tmpl_prototype-progress.ts", content: "import { z } from \"zod\";\n\nconst PrototypeProgressEntrySchema = z.object({\n  use_case_id: z.string(),\n  status: z.enum([\"pending\", \"failed\", \"completed\"]),\n  attempt_count: z.number().int().nonnegative(),\n  last_agent_exit_code: z.number().int().nullable(),\n  quality_checks: z.array(\n    z.object({\n      command: z.string(),\n      exit_code: z.number().int(),\n    }),\n  ),\n  last_error_summary: z.string(),\n  updated_at: z.string(),\n  audit_artifact_path: z.string().optional(),\n  refactor_report_path: z.string().optional(),\n});\n\nexport const PrototypeProgressSchema = z.object({\n  entries: z.array(PrototypeProgressEntrySchema),\n});\n\nexport type PrototypeProgress = z.infer<typeof PrototypeProgressSchema>;\n" },
  { relativePath: "schemas/tmpl_state.ts", content: "import { z } from \"zod\";\n\nconst iterationId = z.string().regex(/^\\d{6}$/);\nconst phase = z.enum([\"define\", \"prototype\", \"refactor\"]);\nconst iso8601 = z.string(); // TODO: tighten to ISO 8601 regex if desired\n\nconst statusFile = z.object({\n  status: z.string(),\n  file: z.string().nullable(),\n});\n\nconst definePhase = z.object({\n  requirement_definition: z.object({\n    status: z.enum([\"pending\", \"in_progress\", \"approved\"]),\n    file: z.string().nullable(),\n  }),\n  prd_generation: z.object({\n    status: z.enum([\"pending\", \"completed\"]),\n    file: z.string().nullable(),\n  }),\n});\n\nconst prototypePhase = z.object({\n  prototype_creation: statusFile.extend({\n    status: z.enum([\"pending\", \"in_progress\", \"completed\"]),\n  }).optional(),\n  prototype_audit: statusFile.extend({\n    status: z.enum([\"pending\", \"in_progress\", \"completed\", \"failed\"]),\n  }).optional(),\n  prototype_refactor: statusFile.extend({\n    status: z.enum([\"pending\", \"in_progress\", \"completed\"]),\n  }).optional(),\n  prototype_approval: statusFile.extend({\n    status: z.enum([\"pending\", \"approved\", \"completed\"]),\n  }).optional(),\n  // DEPRECATED: kept for backward compatibility with older command flows.\n  project_context: statusFile.extend({\n    status: z.enum([\"pending\", \"pending_approval\", \"created\"]),\n  }).optional(),\n  // DEPRECATED: kept for backward compatibility with older command flows.\n  test_plan: statusFile.extend({\n    status: z.enum([\"pending\", \"pending_approval\", \"created\"]),\n  }).optional(),\n  // DEPRECATED: kept for backward compatibility with older command flows.\n  tp_generation: z.object({\n    status: z.enum([\"pending\", \"created\"]),\n    file: z.string().nullable(),\n  }).optional(),\n  // DEPRECATED: replaced by prototype_creation.\n  prototype_build: statusFile.extend({\n    status: z.enum([\"pending\", \"in_progress\", \"created\"]),\n  }).optional(),\n  // DEPRECATED: kept for backward compatibility with older command flows.\n  test_execution: statusFile.extend({\n    status: z.enum([\"pending\", \"in_progress\", \"completed\", \"failed\"]),\n  }).optional(),\n  // DEPRECATED: replaced by prototype_approval.\n  prototype_approved: z.boolean().optional(),\n});\n\n// DEPRECATED: legacy phase, kept for backward compatibility with older command flows.\nconst refactorPhase = z.object({\n  evaluation_report: z.object({\n    status: z.enum([\"pending\", \"created\"]),\n    file: z.string().nullable(),\n  }).optional(),\n  refactor_plan: z.object({\n    status: z.enum([\"pending\", \"pending_approval\", \"approved\"]),\n    file: z.string().nullable(),\n  }).optional(),\n  refactor_execution: z.object({\n    status: z.enum([\"pending\", \"in_progress\", \"completed\"]),\n    file: z.string().nullable(),\n  }).optional(),\n  changelog: z.object({\n    status: z.enum([\"pending\", \"created\"]),\n    file: z.string().nullable(),\n  }).optional(),\n});\n\nconst historyEntry = z.object({\n  iteration: iterationId,\n  archived_at: iso8601,\n  archived_path: z.string(),\n});\n\nexport const StateSchema = z.object({\n  current_iteration: iterationId,\n  current_phase: phase,\n  flow_guardrail: z.enum([\"strict\", \"relaxed\"]).optional(),\n  phases: z.object({\n    define: definePhase,\n    prototype: prototypePhase,\n    refactor: refactorPhase,\n  }),\n  last_updated: iso8601,\n  updated_by: z.string().optional(),\n  history: z.array(historyEntry).optional(),\n});\n\nexport type State = z.infer<typeof StateSchema>;\n" },
  { relativePath: "schemas/tmpl_validate-progress.ts", content: "import { ProgressSchema } from \"./tmpl_progress\";\nimport { readFile } from \"node:fs/promises\";\nimport { join } from \"node:path\";\n\nconst path = join(import.meta.dir, \"..\", \".agents\", \"flow\", \"tmpl_it_000001_progress.example.json\");\nconst raw = JSON.parse(await readFile(path, \"utf-8\"));\nconst result = ProgressSchema.safeParse(raw);\nif (result.success) {\n  console.log(\"tmpl_it_000001_progress.example.json is valid\");\n} else {\n  console.error(\"Validation failed:\", result.error.flatten());\n  process.exitCode = 1;\n}\n" },
  { relativePath: "schemas/tmpl_validate-state.ts", content: "import { StateSchema } from \"./tmpl_state\";\nimport { readFile } from \"node:fs/promises\";\nimport { join } from \"node:path\";\n\nconst path = join(import.meta.dir, \"..\", \".agents\", \"tmpl_state.example.json\");\nconst raw = JSON.parse(await readFile(path, \"utf-8\"));\nconst result = StateSchema.safeParse(raw);\nif (result.success) {\n  console.log(\"tmpl_state.example.json is valid\");\n} else {\n  console.error(\"tmpl_state.example.json validation failed:\", result.error.flatten());\n  process.exitCode = 1;\n}\n" },
  { relativePath: "tmpl_AGENTS.md", content: "# Agents entry point\n\n- **What this project is:** (Describe your product or project. One or two sentences. This file is the single entry point for the agent.)\n- **How to work here:** Use this file as the single entry point. Follow the process phases in order; read and update `.agents/state.json` for the current iteration and phase. Invoke the skills under `.agents/skills/` as indicated by each NVST command. All iteration artifacts live in `.agents/flow/` with the naming `it_` + 6-digit iteration (e.g. `it_000001_product-requirement-document.md`). From the second iteration onward, adhere to [`.agents/PROJECT_CONTEXT.md`](.agents/PROJECT_CONTEXT.md).\n- **Process:** Define/Refine/Approve Requirement → Create Prototype → Audit Prototype → Refactor Prototype → Approve Prototype (see `docs/nvst-flow/` or package documentation).\n- **Project context:** [`.agents/PROJECT_CONTEXT.md`](.agents/PROJECT_CONTEXT.md) — conventions and architecture; the agent adheres from the second iteration onward.\n- **Rule:** All generated resources in this repo must be in English.\n" },
];
