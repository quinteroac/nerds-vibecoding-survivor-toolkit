# Create Test Plan

Generate a test plan for the current prototype iteration.

Requirements:
- Produce a Markdown document named `it_{iteration}_test-plan.md`.
- Write the file under `.agents/flow/`.
- Cover test scope, acceptance checks, and execution strategy aligned to `.agents/PROJECT_CONTEXT.md`.
- Keep all output in English.
