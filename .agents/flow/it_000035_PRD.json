{
  "goals": [
    "Let the user choose between project-level and global skill installation via the interactive `npx skills add` flow.",
    "Install NVST framework skills through `npx skills add`.",
    "Source the installable skills from the repository's `nvst-skills` folder.",
    "Migrate existing `tmpl_` skill files into `nvst-skills` and remove them from `scaffold/.agents/skills/`.",
    "Finish `nvst init` with the skills installed in the chosen location."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Choose where NVST skills are installed",
      "description": "As a user running `nvst init`, I want the skills installer to guide me through scope selection so that I can decide whether skills land in my project or globally without `nvst init` needing to implement its own scope prompt.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`nvst init` delegates scope selection entirely to `npx skills add` — it does not present its own project-vs-global prompt."
        },
        {
          "id": "US-001-AC02",
          "text": "The `npx skills add` interactive flow is presented to the user during `nvst init` so they can choose scope."
        },
        {
          "id": "US-001-AC03",
          "text": "If the user aborts the `npx skills add` interaction (e.g. Ctrl+C), `nvst init` exits cleanly without leaving a partially installed state."
        },
        {
          "id": "US-001-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Install skills through the `skills` package",
      "description": "As a user running `nvst init`, I want the command to invoke the `skills` installer so that NVST skills are installed using the package-based workflow instead of the old static scaffold mechanism.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`nvst init` invokes `npx skills add <nvst-skills-path>` as part of the init workflow, without passing a scope flag (scope is chosen interactively by the user within the `skills` CLI)."
        },
        {
          "id": "US-002-AC02",
          "text": "The installer uses the repository's `nvst-skills` folder as the source of the NVST framework skills."
        },
        {
          "id": "US-002-AC03",
          "text": "The install step completes successfully before `nvst init` exits."
        },
        {
          "id": "US-002-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Complete init with skills installed in the selected location",
      "description": "As a user finishing `nvst init`, I want the selected skills installation to be present at the end of the command so that the project is ready to use immediately.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "After a successful run, the selected scope contains the NVST skills that were installed by the `skills` package."
        },
        {
          "id": "US-003-AC02",
          "text": "The command reports successful completion only after the skill installation step has finished."
        },
        {
          "id": "US-003-AC03",
          "text": "Running `nvst init` with an existing project does not leave the workflow in a partially installed state."
        },
        {
          "id": "US-003-AC04",
          "text": "Running `nvst init` on an already-initialised project does not duplicate or corrupt previously installed skills (the `skills` package handles idempotency, or init detects and skips the install step)."
        },
        {
          "id": "US-003-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Migrate `tmpl_` skill files to `nvst-skills`",
      "description": "As a maintainer of this repository, I want the NVST skill files to live under `nvst-skills/` instead of `scaffold/.agents/skills/` so that the `skills` package can distribute them and the old static-copy mechanism is no longer used for skills.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "All `tmpl_SKILL.md` files are moved from `scaffold/.agents/skills/` to the corresponding paths under `nvst-skills/`, with the `tmpl_` prefix removed."
        },
        {
          "id": "US-004-AC02",
          "text": "The `scaffold/.agents/skills/` directory contains no `tmpl_SKILL.md` files after the migration."
        },
        {
          "id": "US-004-AC03",
          "text": "The `nvst-skills/` folder contains one `SKILL.md` per NVST framework skill."
        },
        {
          "id": "US-004-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "`nvst init` must invoke `npx skills add` and pass through its interactive session to the user; scope selection is handled by the `skills` CLI, not by `nvst init`."
    },
    {
      "id": "FR-2",
      "description": "`nvst init` must invoke `npx skills add` during the init workflow instead of copying `tmpl_` skill files directly."
    },
    {
      "id": "FR-3",
      "description": "The `skills` installer must read NVST skills from the repository's `nvst-skills` folder."
    },
    {
      "id": "FR-4",
      "description": "The init workflow must complete only after the selected installation scope has finished installing the skills."
    },
    {
      "id": "FR-5",
      "description": "The `tmpl_` skill files must be removed from `scaffold/.agents/skills/` and placed under the `nvst-skills/` folder as part of this iteration."
    }
  ]
}
