{
  "goals": [
    "Allow `define requirement` to be invoked N times per iteration, each run producing a new PRD file with an `_NNN` index suffix.",
    "Extend `state.json` to hold an array of PRD entries, each with its own `status` and `file`.",
    "Propagate the multi-PRD model through `approve requirement`, `create prototype`, `audit prototype`, `refactor prototype`, and `approve prototype`.",
    "Preserve backward compatibility: existing single-PRD iterations continue to work without changes.",
    "All iteration artifacts (PRDs, audit reports, refactor plans) use the `_NNN` suffix pattern."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Run `define requirement` multiple times",
      "description": "As a developer, I want to run `nvst define requirement` N times in the same iteration so that each run produces a separate PRD file (`it_000044_product-requirement-document_001.md`, `_002.md`, …).",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "First `define requirement` run creates `it_XXXXXX_product-requirement-document_001.md` in `.agents/flow/`."
        },
        {
          "id": "US-001-AC02",
          "text": "Second run (and each subsequent run) creates `it_XXXXXX_product-requirement-document_002.md`, `_003.md`, etc., without overwriting existing files."
        },
        {
          "id": "US-001-AC03",
          "text": "`state.json` `phases.define.requirement_definition` becomes an array; each entry has `{ index, status, file }`."
        },
        {
          "id": "US-001-AC04",
          "text": "Running `define requirement` after `approve requirement` is blocked by the guardrail (status is `approved`), unless `--force` is passed."
        },
        {
          "id": "US-001-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "`approve requirement` approves all pending PRDs",
      "description": "As a developer, I want `nvst approve requirement` to approve all PRD entries in one command so that I don't need to approve each PRD individually.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "Command sets `status = \"approved\"` on every entry in the `requirement_definition` array."
        },
        {
          "id": "US-002-AC02",
          "text": "If any entry is already `approved`, the command skips it (idempotent)."
        },
        {
          "id": "US-002-AC03",
          "text": "Confirmation prompt (or `--yolo` flag) lists all PRDs that will be approved before proceeding."
        },
        {
          "id": "US-002-AC04",
          "text": "`state.json` is persisted with all entries marked `approved` after success."
        },
        {
          "id": "US-002-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "`create prototype` iterates over all PRDs and their user stories",
      "description": "As a developer, I want `nvst create prototype` to process every approved PRD in sequence (PRD1→UC1…UCn, PRD2→UC1…UCn, …) so that all features are scaffolded in a single command run.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "Command reads all PRD entries from `state.json` and processes them in index order."
        },
        {
          "id": "US-003-AC02",
          "text": "For each PRD, the agent prompt includes that PRD's content and iterates through its user stories."
        },
        {
          "id": "US-003-AC03",
          "text": "`state.json` `phases.prototype.prototype_creation` becomes an array with one entry per PRD (`{ index, status, file }`)."
        },
        {
          "id": "US-003-AC04",
          "text": "Each prototype creation artifact is named `it_XXXXXX_prototype-creation_NNN.md` (or equivalent)."
        },
        {
          "id": "US-003-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "`audit prototype` produces one audit report per PRD",
      "description": "As a developer, I want `nvst audit prototype` to validate each PRD independently and produce one audit report per PRD so that issues are traceable back to a specific requirement.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "Command produces N audit report files: `it_XXXXXX_audit-report_001.json`, `_002.json`, etc."
        },
        {
          "id": "US-004-AC02",
          "text": "`state.json` `phases.prototype.prototype_audit` becomes an array with one entry per PRD (`{ index, status, file }`)."
        },
        {
          "id": "US-004-AC03",
          "text": "Each audit report is associated with its source PRD index."
        },
        {
          "id": "US-004-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "`refactor prototype` runs one refactor pass per audit report",
      "description": "As a developer, I want `nvst refactor prototype` to execute a refactor pass for each audit report so that all detected issues across all PRDs are addressed.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "Command iterates over all audit report entries in `state.json` and runs a refactor pass for each."
        },
        {
          "id": "US-005-AC02",
          "text": "Refactor artifacts are named `it_XXXXXX_refactor-plan_001.md`, `_002.md`, etc."
        },
        {
          "id": "US-005-AC03",
          "text": "`state.json` `phases.prototype.prototype_refactor` becomes an array with one entry per audit report."
        },
        {
          "id": "US-005-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-006",
      "title": "Backward compatibility — single-PRD iterations continue to work",
      "description": "As a developer, I want existing single-PRD iteration workflows to work without any changes so that upgrading NVST does not break current projects.",
      "acceptanceCriteria": [
        {
          "id": "US-006-AC01",
          "text": "A project with a `state.json` that has `requirement_definition` as a single object (legacy) is either auto-migrated or gracefully handled by the Zod schema."
        },
        {
          "id": "US-006-AC02",
          "text": "All commands that read `requirement_definition` handle both the legacy scalar shape and the new array shape."
        },
        {
          "id": "US-006-AC03",
          "text": "A manually executed single-PRD end-to-end run (define → approve → create → audit → refactor) succeeds without errors."
        },
        {
          "id": "US-006-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-007",
      "title": "`auto` mode supports multi-PRD flow",
      "description": "As a developer, I want the `nvst auto` command to work correctly when multiple PRDs exist so that autonomous runs process all PRDs without manual intervention.",
      "acceptanceCriteria": [
        {
          "id": "US-007-AC01",
          "text": "`auto` mode loops through all PRD entries and dispatches each phase command for each PRD in order."
        },
        {
          "id": "US-007-AC02",
          "text": "State transitions in `auto` mode correctly advance each per-PRD entry status."
        },
        {
          "id": "US-007-AC03",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-008",
      "title": "`approve prototype` consolidates all PRDs into a single CHANGELOG entry and PR",
      "description": "As a developer, I want `nvst approve prototype` to build the CHANGELOG entry and GitHub PR body from all PRDs in the iteration (not just one) so that every feature defined in the iteration is documented and submitted for review in a single step.",
      "acceptanceCriteria": [
        {
          "id": "US-008-AC01",
          "text": "Command reads all PRD entries from `state.json`'s `requirement_definition` array (in index order) instead of a single hardcoded PRD file."
        },
        {
          "id": "US-008-AC02",
          "text": "The CHANGELOG entry (`## [iteration] - YYYY-MM-DD`) includes a `### Added` section with goal bullets merged from every PRD's `## Goals` section, each group prefixed with the PRD title (e.g. `**PRD 001 — Title:** …`)."
        },
        {
          "id": "US-008-AC03",
          "text": "The GitHub PR body (created via `gh pr create`) includes the Requirement name, Context, and Goals sections from each PRD, concatenated in index order, followed by the refactor report path(s) and the standard NVST footer."
        },
        {
          "id": "US-008-AC04",
          "text": "If `gh` CLI is unavailable, the command warns and skips PR creation without corrupting `CHANGELOG.md` (existing behavior preserved)."
        },
        {
          "id": "US-008-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** `define requirement` must auto-increment the PRD index by inspecting existing entries in `state.json`'s `requirement_definition` array. Index is zero-padded to 3 digits (e.g. `001`, `002`)."
    },
    {
      "id": "FR-2",
      "description": "** All artifact filenames for multi-PRD phases must follow the pattern `it_XXXXXX_<artifact-type>_NNN.<ext>` where `NNN` matches the PRD index."
    },
    {
      "id": "FR-3",
      "description": "** The `StateSchema` Zod definition must be updated so `requirement_definition` accepts an array of `{ index: string, status: enum, file: string | null }` objects."
    },
    {
      "id": "FR-4",
      "description": "** The Zod schema update must preserve backward compatibility: the legacy single-object shape (no `index` field, flat `status`/`file`) must either be accepted via a union type or migrated at read time."
    },
    {
      "id": "FR-5",
      "description": "** `approve requirement` must operate on all entries with `status !== \"approved\"` and must surface the list of affected PRD files before confirming."
    },
    {
      "id": "FR-6",
      "description": "** `create prototype`, `audit prototype`, and `refactor prototype` must each store their results as arrays in `state.json`, one element per source PRD index."
    },
    {
      "id": "FR-7",
      "description": "** `auto` mode must be updated to iterate over per-PRD entries at every relevant phase."
    },
    {
      "id": "FR-8",
      "description": "** The scaffold template `state.json` (used by `nvst init`) must initialize `requirement_definition` as an empty array `[]`."
    },
    {
      "id": "FR-9",
      "description": "** `approve prototype` must read all PRD entries from `state.json`'s `requirement_definition` array and consolidate their `## Goals` bullet lists into a single `### Added` block in `CHANGELOG.md`, with each PRD's goals grouped under its title. The GitHub PR body (created via `gh pr create`) must include the Context and Goals sections from each PRD concatenated in index order, plus the refactor report path(s) and the standard NVST footer."
    }
  ]
}
